"""
COMPAREX Backend – User Service (Stub)

Full implementation in Phase 2.
"""

from app.core.logging import get_logger

logger = get_logger(__name__)


class UserService:
    """
    User management service — profile CRUD, preferences, etc.

    Phase 2 Implementation Plan:
    - get_user(user_id) -> UserPublic
    - update_profile(user_id, data) -> UserPublic
    - delete_account(user_id) -> None
    - change_password(user_id, old, new) -> None
    """

    pass  # Phase 2
